# Prepare esm-treeshake-lab git remote

Type: task
Status: open

## Question

What exact steps (or completed state) get this repo onto `git@github.com:atiq-playground/esm-treeshake-lab.git` as its remote, without rewriting unrelated history policy?

Record: whether the GitHub repo must be created first, remote name (`origin`), and any rename from the current local folder/`next-app` package identity that should wait until migration handoff. This unblocks later implementation remotes; it does not perform the monorepo migration.

# Lock Nx + Bun bootstrap shape

Type: grilling
Status: open

## Question

What concrete Nx + Bun workspace bootstrap do we hand off: root workspace config location/format for Bun, `nx.json` / project naming (`web`, `service-core`, etc.), default targets (`build`, `dev`, `typecheck`, `test`), and root `package.json` script wrappers?

Standing preference is already Nx + Bun workspaces with the agreed folder globs — this ticket locks the bootstrap specifics so implementation does not improvise.

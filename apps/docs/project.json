{
  "name": "@apps/docs",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/docs",
  "targets": {
    "dev": {
      "command": "bun run highlight:packaging && next dev --port 3000",
      "options": { "cwd": "apps/docs" }
    },
    "build": {
      "command": "bun run highlight:packaging && next build",
      "options": { "cwd": "apps/docs" }
    },
    "start": {
      "command": "next start",
      "options": { "cwd": "apps/docs" }
    },
    "preview": {
      "command": "bun run highlight:packaging && opennextjs-cloudflare build && opennextjs-cloudflare preview",
      "options": { "cwd": "apps/docs" }
    },
    "deploy": {
      "command": "bun run highlight:packaging && opennextjs-cloudflare build && opennextjs-cloudflare deploy",
      "options": { "cwd": "apps/docs" }
    }
  }
}
